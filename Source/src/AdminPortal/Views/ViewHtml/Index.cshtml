@using System.IO
@using System.Text.RegularExpressions
@using System.Security.Principal
@*@{
    ViewBag.Title = "?";
}*@

@if (User.Identity.IsAuthenticated)
{
    string content = @ViewData["HtmlContent"] as string;
    <div>@Html.Raw(content)</div>

    @*@Html.Raw(File.ReadAllText(Server.MapPath("~/Views/ViewHtml/" + ViewData["PageName"])))*@
}

